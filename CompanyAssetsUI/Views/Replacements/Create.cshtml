@model CompanyAssetsUI.Models.Replacement

@{
    ViewData["Title"] = "Create";
}

<span class="display-4 text-info">Create</span><span> new replacement</span>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CatalogueID" class="control-label"></label>
                <select asp-for="CatalogueID" class="form-control" asp-items="ViewBag.CatalogueID"></select>
            </div>
            <div class="form-group">
                <label asp-for="ReplacingID" class="control-label"></label>
                <select asp-for="ReplacingID" class="form-control" asp-items="ViewBag.ReplacingID"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-info" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="text-info">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
